{
  "name": "cashier-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "mkdir -p dist && cp index.html dist/",
    "preview": "vite preview"
  }
}
